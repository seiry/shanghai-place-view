name: Scheduled Task

on:
  schedule:
    - cron: '*/10 * * * *'  # 每十分钟触发一次任务

jobs:
  run_task:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run curl command
        run: curl ${{ secrets.CronUrl }}
